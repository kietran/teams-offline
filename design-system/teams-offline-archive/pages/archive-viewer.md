# Page Override: Archive Viewer

This page overrides the generated Master where rules conflict.

- Pattern: desktop three-pane archive reader; no dashboard.
- Navigation: `Sao lưu` and `Kho lưu trữ`; archive is active. Remove the old
  `Cập nhật từ Teams` API action.
- Colors: `#F8FAFC` canvas, white surface, `#1E293B` text, `#64748B` muted,
  `#E2E8F0` dividers, `#2563EB` primary, `#EFF6FF` selected.
- Typography: system UI / Segoe UI / Noto Sans; no remote font.
- Layout: channel rail, search/results list, conversation detail.
- Controls: one search field and labeled `Mở tệp` actions; visible focus.
- Offline status uses check icon plus `Có thể xem ngoại tuyến`.
- Forbidden: composer, editing, tags, notes, charts, metrics, advanced filters,
  gradients, glassmorphism, decorative cards, or excessive icons.
- Concept: `docs/design/concepts/archive-viewer-v2.png`.
